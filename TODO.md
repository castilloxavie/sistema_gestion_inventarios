# TODO: Corregir funcionalidades del módulo inventory

## Pasos completados:

1. ✅ Renombrar archivo `inventoryCntroller.js` a `inventoryController.js`.
2. ✅ Actualizar `inventoryController.js`: corregir nombres de métodos (creta -> create, getBiId -> getById), agregar export class, corregir logs de consola, cambiar llamada a getMovement por getMovementById.
3. ✅ Actualizar `inventoryServices.js`: corregir "atributes" a "attributes", agregar método getMovementById.
4. ✅ Actualizar `inventoryRouter.js`: corregir import path, corregir instanciación del controller, corregir llamada al método createMovement a create.
5. ✅ Verificar que las rutas funcionen correctamente después de los cambios.
