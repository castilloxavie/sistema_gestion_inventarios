# Configuracion de la base de datos
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=inventory_system

# Configuracion del servidor
PORT=3000
NODE_ENV=development

# Token de seguridad para JWT
SECURITY_TOKEN_JWT=secret
